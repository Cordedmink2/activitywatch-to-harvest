[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    bench: timing tripwire, not a correctness test — skipped unless --bench is passed
    scenario: end-to-end run of a whole synthetic day against the fake ActivityWatch
filterwarnings =
    error
