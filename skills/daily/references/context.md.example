# Timesheet context

The billables `daily` skill reads this file every run. It's your source of truth for
*who's who* and *which client each activity belongs to*. Keep only confirmed facts;
the skill will propose additions when it learns something new — review and merge
those additions rather than letting them accumulate elsewhere.

You can hand-edit this file at any time. The skill never overwrites it without
showing you the diff first.

Keep it lean: only *your* facts belong here — generic heuristics live in the skill
itself, and the skill checks this file against a size budget (default 14,000 bytes,
see SKILL.md Step 11) after every edit and compacts when it's exceeded.

---

## Preferences

Override the skill's defaults here. Omit any line you're happy with as default.

Your timezone is *not* here: it is machine configuration, set once at install
(`/plugin configure billables` → `TIMESHEET_TIMEZONE`), so it can't drift out of step with
a second workspace. Everything below is a judgement call about *your* working style, which
is why it lives with your facts rather than in a shipped script.

- **AFK threshold for a real break**: `1050s` (17.5 min) — shorter AFK folds into work
  <!-- afk_blocks --afk-threshold -->
- **Substantive activity floor**: `120s` — a shorter not-afk run is a flicker, not work
  <!-- afk_blocks --solid; lower it if your real work arrives in short bursts -->
- **End-of-day blip gap**: `600s` — this long after the last substantive activity, the
  day's last moment is a mouse nudge, not work  <!-- afk_blocks --blip-gap -->
- **Smallest uncovered stretch worth flagging**: `900s` (15 min) — below this, activity
  the proposed blocks miss is rounding  <!-- afk_blocks --min-uncovered -->
- **Active / thin bands**: at or above `0.7` active_ratio a stretch is billable, below
  `0.4` it is mostly idle  <!-- afk_blocks --active-band / --thin-band -->
- **Timeline noise floor**: `5s` — shorter window events are tab-switch noise
  <!-- activity_timeline --noise-floor -->
- **Timeline gap fold**: `60s` — a shorter gap doesn't break a span in two
  <!-- activity_timeline --gap-fold -->
- **Smallest span the timeline prints**: `3.0 min` — shorter ones are hidden from the text
  view but still counted in the rollup  <!-- activity_timeline --min-span -->
- **Minimum billable block**: `0.25 hr` — nothing shorter is posted as its own entry
  <!-- no flag: the model applies this when it drafts, per references/output-format.md -->
- **Short-day floor**: `6.0 hrs` — a day billed under this is one the month-end sweep looks
  at. Triage only: it decides which days get investigated, never which were billed short —
  that comparison is against the day's own active minutes
  <!-- no flag: the reconcile skill applies this when it sweeps a month -->
- **Lunch detection window**: `11:30`–`14:30` local — longest AFK in here is lunch
- **Work-hours window for AW rendering**: `06:00`–`20:00` local
- **Default task for client work**: `<the task most of your client work bills to>` — spelled
  exactly as your timesheet provider spells it
- **Default task for internal/admin time**: `<your non-billable catch-all task>`
- **Display name in window titles**: `<YourFirstName>` (used by Edge profile signal — e.g. titles ending `… - <Profile> - <YourFirstName> - Microsoft Edge`)

Each `<!-- ... -->` names the flag the skill passes when you change the value beside it.
Changing a number here is the supported way to retune the skill; editing the constant in a
shipped script is not — an update overwrites it.

## AW buckets

(The skill caches these after first discovery so subsequent runs skip the buckets endpoint.)

- Hostname suffix: `<YOUR-MACHINE-NAME>`  <!-- e.g. WS-113359 -->
- Window: `aw-watcher-window_<HOST>`
- AFK: `aw-watcher-afk_<HOST>`
- Web (Firefox): `aw-watcher-web-firefox_<HOST>`
- Web (Edge/Chrome): `aw-watcher-web-chrome_<HOST>`
- VS Code: `aw-watcher-vscode_<HOST>` (optional)

---

## Internal colleagues

Names that, if they appear in Teams chat/call titles, indicate internal work
rather than a client.

- **<Full Name>** — <role / team, optional>
- **<Full Name>** — <role / team, optional>

## Known external contacts

Teams often marks externals in window titles like `Chat | <Name> (External unfamiliar) | …`.
List the externals you regularly chat with so they auto-classify to a client.

- **<Full Name>** — external. Project: <client name>. <Optional note: "Teams sometimes flags as External unfamiliar — that's stale.">
- **<Full Name>** — external. Project: <client name>.

---

## Active client projects

One section per client. Include every signal that would let the skill identify
"this block is for <client>" without asking you.

### <Client Name>

**Client name**: `<exact name as your timesheet provider shows it>`
**Default project**: `<project code>` `<project name>`

Signals:
- Work-item prefixes: `<PREFIX>` (e.g. numbers like `<PREFIX>1234` are this client's)
- `<token>` appears in URL or window title
- Edge profile `<ProfileName>` (window title ends `... - <ProfileName> - <YourName> - Microsoft Edge`)
- Dynamics 365 at `<env-url>.crm6.dynamics.com`
- ChatGPT project named `<Project Name>` (Firefox tab title `ChatGPT - <Project Name>`, URL contains `g-p-…-<slug>`)
- Azure DevOps org `<dev.azure.com/orgname>`
- SharePoint subdomains: `<subdomain>.sharepoint.com`
- VS Code workspace `<WorkspaceName>` (title ends `… - <WorkspaceName> - Visual Studio Code`)
- Local repo path `<path>\<repo>`
- Microsoft Teams team `<Team Name>`

Common activities: <free text describing what kind of work you typically do for this client>

### <Client Name 2>

…repeat the structure above for each client.

---

## Shared signals across client groups (optional)

If two clients share signals (sister organisations, shared codebase, etc.), document
the shared patterns here and the rule for splitting.

### <Parent group name>

<Client A> and <Client B> are both part of <Parent>. Some artefacts span both:

- **Recurring `<Meeting Name>`** → bills to <project> (the convention you actually use)
- **Codebase `<repo>`** → serves both clients; pick by work item / commit context

---

## Work kinds

**This table is what makes the skill work against your provider.** The shipped rubric
decides one of seven neutral *work kinds* for each block — it can't know what your
account calls them, because no two accounts spell their tasks the same way. Fill in the
right-hand column with your provider's exact task names and the mapping is done.

Leave a row blank and that work kind falls back to the default task above. Leave the whole
table out and the skill matches each work kind against the tasks your projects actually
offer, then proposes the row it worked out — so an existing `.context.md` keeps working and
fills this in as you go.

| Work kind | Your task |
|---|---|
| `Meeting` | `<task name>` |
| `Development` | `<task name>` |
| `Documentation` | `<task name>` |
| `Project management` | `<task name>` |
| `Testing` | `<task name>` |
| `Investigation` | `<task name>` |
| `Internal admin` | `<a non-billable task>` |

A name here only decides *which* task to reach for. Whether it exists on a given project,
and whether it's billable, comes from that project's own task assignments — the skill
checks, and substitutes with a note when your task isn't offered there.

## How I bill (overrides + examples)

The generic conventions (client-readable outcome-focused notes, bill by work item not
by window, stale-item→new-item, catalog caveats) live in the skill's
classification rubric — don't restate them here. Only *your* overrides and voice:

Real note examples from your entries (paste 3–5 here once you have them):
- "<example outcome description>"
- "<example outcome description>"
- "<example outcome description>"

Per-project conventions:
- **<Project code> default task**: `<task name>` — <when it applies>
- **Codebase `<repo path>`** serves multiple projects (`<code>`, `<code>`) — pick by work item
- **Shared prefix**: `<PREFIX>` spans several clients — resolve it from the assignment catalog,
  not from the prefix alone

---

## Personal browsing patterns to exclude (do not bill)

The rubric's generic categories (travel, social media, news/shopping/email, timer
overlays, home admin) already apply. List only the *specific* recognizers seen on
your machine — specific patterns avoid false positives:

- <site / search pattern, e.g. "Google Flights 'Christchurch to …'">
- <recurring personal page>

---

## Notes on inference

Standing user-specific rules that don't fit the categories above. Examples:

- **The timesheet provider's own UI** (its tab in the browser) → `<your internal-admin project>`, not billable client work.
- **Internal-admin project** for the rubric's terminal-adjacency rule = `<project>`. Timesheet-automation paths on this machine: `<paths>`.
- **Recurring meeting `<name>`** → `<project · task>` (your canonical attribution).
